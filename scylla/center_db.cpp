

#include <stdexcept>

namespace center_db {
void init() {
    /*
        Init scylla client

        Must throw std::runtime_error if fail to init
    */

    throw std::runtime_error("Failed to init Center Db");
}
}  // namespace center_db
